# EduNotas — Checklist maestra y roadmap

Checklist maestra con lo implementado, lo pendiente y lo descartado (con motivo). Al terminar algo, se marca en esta lista.

## Roadmap / Checklist de EduNotas

| Prioridad   | Área            | Funcionalidad / tarea                                   | Estado          |
| ----------- | --------------- | ------------------------------------------------------- | --------------- |
| 🔴 Alta     | Seguridad       | Rate limiting en login                                  | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Rate limiting por endpoint sensible (admin/grade)       | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Verificar permisos de profesor sobre sus propios cursos | ✅ Verificado   |
| 🔴 Alta     | Seguridad       | Verificar permisos de estudiante sobre sus cursos       | ✅ Verificado   |
| 🔴 Alta     | Seguridad       | Protección contra IDOR / acceso a recursos ajenos       | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Refresh token rotation                                  | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Refresh token blacklist                                 | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Evitar información sensible en `localStorage`           | ✅ Implementado |
| 🔴 Alta     | Seguridad       | CSRF                                                    | ✅ Implementado |
| 🔴 Alta     | Seguridad       | CORS                                                    | ✅ Implementado |
| 🔴 Alta     | Seguridad       | CSP                                                     | ✅ Implementado |
| 🔴 Alta     | Seguridad       | HSTS                                                    | ✅ Implementado |
| 🔴 Alta     | Auditoría       | Log de cambios sensibles                                | ✅ Implementado |
| 🔴 Alta     | Auditoría       | Registrar cambios de calificaciones                     | ✅ Implementado |
| 🔴 Alta     | Auditoría       | Registrar cambios de cursos                             | ✅ Implementado |
| 🔴 Alta     | Auditoría       | Registrar cambios de usuarios/roles                     | ✅ Implementado |
| 🔴 Alta     | Auditoría       | Auditar alta de inscripción y cambios de tarea (published/max_score) | ✅ Implementado |
| 🔴 Alta     | Historial       | Snapshot automático al borrar sección/curso (inmutable) | ✅ Implementado |
| 🔴 Alta     | Observabilidad  | Error tracking in-house (ErrorLog + EXCEPTION_HANDLER + ErrorBoundary) | ✅ Implementado |
| 🔴 Alta     | DevOps          | GitHub Actions: lint + tests + coverage + `makemigrations --check` + escaneo de dependencias (npm/pip) (sin deploy) | ✅ Implementado |
| 🔴 Alta     | Calificaciones  | Gradebook                                               | ✅ Implementado |
| 🔴 Alta     | Calificaciones  | Cálculo de nota final                                   | ✅ Implementado |
| 🔴 Alta     | Calificaciones  | Ponderaciones por tarea/examen                          | ✅ Implementado |
| 🔴 Alta     | Calificaciones  | Ponderación de nota final por curso (toggle; acumulados ponderados + exámenes; 2 parciales) | ✅ Implementado |
| 🔴 Alta     | Calificaciones  | Historial de calificaciones                             | ✅ Implementado |
| 🔴 Alta     | Testing         | Tests de autenticación                                  | ✅ Implementado |
| 🔴 Alta     | Testing         | Tests de permisos                                       | ✅ Implementado |
| 🔴 Alta     | Testing         | Tests de API                                            | ✅ Implementado |
| 🔴 Alta     | Testing         | Tests de frontend (Vitest + RTL)                        | ✅ Implementado |
| 🟠 Media    | Testing         | Tests frontend críticos (ProtectedRoute, grading, enrollment, exports) | ✅ Implementado (lote 1) |
| 🟠 Media    | Testing         | Tests de páginas críticas en frontend (Login, Register, CourseDetail, Teams list/detail) | ✅ Implementado (Fase 2) |
| 🟠 Media    | Cursos          | Estados del curso                                       | ✅ Revisado      |
| 🟠 Media    | Cursos          | Archivar cursos                                         | ✅ Implementado |
| 🟠 Media    | Cursos          | Polling de cursos (refetchInterval 30s)                 | ✅ Implementado |
| 🟠 Media    | Inscripciones   | Estados de inscripción                                  | ✅ Implementado |
| 🟠 Media    | Usuarios        | Desactivación de usuarios (`is_active`)                 | ✅ Implementado |
| 🟠 Media    | Notificaciones  | Notificaciones in-app — solo eventos de sistema (calificación publicada, admisión aprobada, solicitud de ingreso) | ✅ Implementado |
| 🟠 Media    | Reportes        | Reporte de curso                                        | ✅ Implementado |
| 🟠 Media    | Reportes        | Exportar CSV/Excel                                      | ✅ Implementado |
| 🟠 Media    | Dashboard       | Dashboard de profesor                                   | ✅ Implementado |
| 🟠 Media    | Dashboard       | Dashboard de estudiante                                 | ✅ Implementado |
| 🟠 Media    | UX/UI           | Estados loading/empty/error                             | ✅ Implementado |
| 🟠 Media    | UX/UI           | Responsive mobile                                       | ✅ Implementado |
| 🟠 Media    | UX/UI           | Design system                                           | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Estandarizar respuestas de error 4xx (DRF, `detail` estable) | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Autenticación de doble factor (TOTP)                     | ⏳ Pendiente   |
| 🔴 Alta     | Seguridad       | Concurrencia: joins/calificaciones simultáneas no duplican | ✅ Implementado |
| 🔴 Alta     | Seguridad       | IDOR: editar/borrar curso ajeno → 404                    | ✅ Verificado   |
| 🟠 Media    | Cursos          | Cierre de curso = bloquear acciones (calificar, inscribirse, equipos) | ✅ Implementado |
| 🟠 Media    | UX/UI           | Detalle de curso archivado: guard de inscripción + badge              | ✅ Implementado |
| 🟠 Media    | Dashboard       | Admin: impersonar desde "Impersonados recientemente" (clic + confirmación) | ✅ Implementado |
| 🔴 Alta     | Seguridad       | Profesores solo ven/sus cursos propios (join/enroll bloqueados aunque sea público) | ✅ Implementado |
| 🟠 Media    | DevOps          | Test de carga con Locust (DISABLE_THROTTLE + read-mix por rol) | ✅ Implementado |

### Y agregaría una categoría especial: arquitectura

| Área         | Tarea                                          | Estado           |
| ------------ | ---------------------------------------------- | ---------------- |
| Arquitectura | Revisar estructura de apps Django              | ✅ Revisado      |
| Arquitectura | Separar lógica de negocio de views             | ✅ Hecho (enrollment) |
| Arquitectura | Servicios/repositories donde realmente aporten | ✅ Revisado      |
| Arquitectura | Manejo global de errores API                   | ✅ Revisado      |
| Arquitectura | Paginación consistente                         | ✅ Implementado  |
| Arquitectura | Cancelación de requests                        | ✅ Implementado  |
| Arquitectura | TanStack Query                                 | ✅ Implementado  |
| Arquitectura | Cache/invalidation strategy                    | ✅ Implementado  |
| Arquitectura | Axios centralizado                             | ✅ Implementado  |
| Arquitectura | Manejo de 401 + refresh                        | ✅ Implementado  |

### Descartados por decisión de producto (no priorizar)

| Funcionalidad                | Motivo                                                                  |
| ---------------------------- | ----------------------------------------------------------------------- |
| Importar estudiantes por CSV | Los estudiantes crean su cuenta y se inscriben/admiten por el flujo actual |
| Exportar a PDF               | Los reportes van a XLSX/CSV porque después se trabajan en Word           |
| Soft delete genérico         | `is_active` + endpoint admin ya cubren la desactivación                  |
| Email notifications          | Sin necesidad actual; toasts in-app sí.                                |
| Push notifications           | Sin necesidad actual                                                   |
| WebSockets / tiempo real     | El polling (30s) cubre la necesidad de frescura                         |
| Redis compartido             | Escala actual no lo requiere (`REDIS_URL` ya es opcional en `.env.example`) |
| Background jobs              | Nada pesado que lo requiera                                             |
| Analytics avanzadas          | Sin métricas de uso decididas                                           |
| Auditoría de teams/sections | Solo se audita lo que afecta calificaciones y admisiones (decisión) |
| `EventLog` en Django admin   | La página frontend de actividad ya permite inspeccionar los logs        |

## Roadmap recomendado (orden de ejecución)

| # | Fase | Alcance | Esfuerzo | Por qué este orden |
|---|---|---|---|---|
| 1 | **CI gate** | `.github/workflows/ci.yml`: job backend (`manage.py check` + `manage.py test`) + job frontend (`oxlint`, `vitest run`, `vite build`); `concurrency` con cancel; **sin deploy** (Render lo hace al mergear `main`) | ~1d | Protege todo lo siguiente: `main` auto-despliega en Render |
| 2 | **Auditoría completa** | ✅ Hecho: logs de alta de inscripción (join/enroll, con status) y cambio de tarea (published/max_score); labels en `activityMeta.js`; **login auditado desde la ronda 6** (ver nota de auditoría de acceso). Fuera de alcance (decisión): teams, sections, Django admin | — | — |
| 3 | **Snapshots por borrado** | ✅ Hecho: señal `pre_delete` en `Section` (con `origin` distingue borrado de curso vs de grupo) → `course/snapshots.capture_section_snapshot()`; modelo `SectionSnapshot` inmutable (headers + payload JSON desnormalizado con curso, profesor, estudiantes/estados, equipos, tareas, notas, nota final y agregados); endpoints `GET /api/snapshots/` (búsqueda + paginación, teacher = propios, superuser = todos) + `export-grades` xlsx / `export-grades-csv` / `grades-report` regenerados desde el payload; vista `Histórico` en el frontend (lista + detalle con descargas) | — | — |
| 4 | **Observabilidad in-house** | ✅ Hecho: `config/errors.py` con `EXCEPTION_HANDLER` (solo loguea 5xx inesperados; 400/401/403/404/405/429 pasan igual que antes) → `ErrorLog` (`error_id` público, source server/client, path, user, clase, message, traceback, status, `error_id_ref`); respuesta JSON estable con `error_id` de soporte; `POST /api/errors/` (AllowAny, `ErrorThrottle` 10/min, sanitizado: sin body, con truncados) + `GET /api/errors/` y detalle (superuser, lista sin traceback); `ErrorBoundary` + listeners `error`/`unhandledrejection` (dedupe y anti-spam); página `Errores` en admin (lista con filtro de fuente + detalle con traceback); sin Sentry | 1–2d | Con más features y merges, detectar errores de prod es crítico |
| 5 | **Notificaciones in-app** | ✅ Hecho: app `notifications` con modelo `Notification` (recipient, `type`, payload, `is_read`, índices) + API (listar paginado propio, `unread-count`, marcar leída, `read-all`; todo scoped al usuario, `unread_only`, cleanup perezoso de leídas >30 días) + triggers en los servicios (grade_student/grade_team solo si la tarea está publicada, admisión aprobada manual y por auto-aceptación, solicitud de ingreso pendiente al profesor) + campana con badge + dropdown (polling 30s) + página `/notifications` con filtro y paginación + `notificationMeta` con tests. **Alcance (decisión):** NO hay "tarea publicada" ni aviso por borrado de sección/curso | 1.5–3d | Valor real pero secundario |
| 6 | **Tests frontend críticos** | Lote 1: `ProtectedRoute` (roles/superuser/block), `useGradeMutations` + validación 0..max, flujo approve/reject/auto-aceptar matrícula, export en `GradesReportPage` (blob). Entremezclado con las fases 3–5 | 1–2d | Protege lo crítico, sin perseguir coverage |
| 7 | **Evolución, progreso y boletín** | Tres features de calificaciones sobre el modelo existente (ver notas abajo) | 1–3d | Mejoras de valor demostrable para el evaluador |

## Notas de la auditoría (revisado contra el código)

- **CSP** en **modo bloqueo** (`Content-Security-Policy`, `config/middleware.py` + `settings.py`, activable por defecto; `CSP_APPLY=False` para desactivar, `CSP_REPORT_URI` para monitorizar además). Se omite en DEBUG. Los hashes `script-src` se calculan dinámicamente sobre los scripts inline del `index.html` servido (immune a cambios de build y saltos de línea).
- **Auditoría:** `EventLog` cubre impersonación, **login** (ver nota de auditoría de acceso abajo), calificaciones, cursos (crear/editar/borrar + `course_settings` + inscripciones approve/reject/eliminar), usuarios/roles (activar/desactivar, cambiar rol), **alta de inscripción** (join/enroll/Create, con status final incluida la auto-aceptación) y **tareas** (update de `max_score`/`is_published` con cambios desde/hacia; los edits cosméticos de título no generan evento). Quedan fuera por decisión: sections y teams. `EventLog` no está registrado en el admin de Django (se inspecciona desde la página de actividad del frontend).
- **Historial de calificaciones:** `GradeHistory` guarda cada cambio (old/new + actor + fecha) y se consulta vía `GET /api/grades/<pk>/history/`; modal en el panel del profesor.
- **Nota final:** cálculo server-side (`grading/final.py`, sin calificar cuenta como 0) expuesto en el dashboard. Con la **ponderación por curso** activada (aquí abajo), la nota final pasa a repartirse entre acumulados y exámenes (ambos promediados por puntos dentro de cada parcial) según los % configurados para el curso.
- **Exportar:** Excel `.xlsx` (openpyxl, con sanitización anti-inyección de fórmulas) y CSV (UTF-8 con BOM, misma sanitización, columna total). No hay PDF (descartado).
- **Estados del curso:** visibilidad (Público/Privado), `is_active` y respaldo de archivar/restaurar (oculta el curso por completo al estudiante; el teacher lo ve para restaurar). Las secciones no tienen `is_closed`: el cierre/histórico llegará con los **snapshots por borrado**.
- **Desactivación de usuarios:** existe `is_active` con endpoint admin; no hay soft-delete con `deleted_at` (descartado).
- **Throttles por endpoint:** `AdminThrottle` (admin 20/min) en impersonate/admin/users/activity, `GradeThrottle` (grade 60/min) en grade-team/grade-student y `ErrorThrottle` (error 10/min, clave por usuario o IP) en el reporte de errores del frontend. `is_active` en el admin ahora exige booleano real (antes `bool("false")` se convertía a `True`).
- **Renovación de sesión:** el interceptor reintenta el refresh tras re-sincronizar el token CSRF si la cookie expiró (403) con la sesión aún viva (p. ej. impersonación inactiva >1 h).
- **Snapshots (decisión de diseño):** se crean **automáticamente al borrar** — señal `pre_delete` de `Section` cubre borrar una sección y, vía cascada, borrar un curso (snapshot por cada sección). El `origin` de Django en la señal distingue `reason` (course_delete vs section_delete). Payload JSON desnormalizado e inmutable; reportes históricos se regeneran desde el snapshot reutilizando `grading/exports.py`. Acceso: el profesor del curso (por `teacher_id` congelado) y superusuarios; los exports congelan la vista oficial (solo tareas publicadas y alumnos aprobados). Si la captura falla, se guarda igual un snapshot degradado y **no** se bloquea el borrado.
- **Notificaciones (decisión):** solo eventos de sistema confirmados por producto: **calificación publicada** (al estudiante, equipo e individual, solo si la tarea está publicada), **admisión aprobada** (al estudiante, manual y auto-aceptación) y **solicitud de ingreso** (al profesor, solo cuando la matrícula queda en PENDING). Fuera de alcance: notificación de tarea publicada, rechazos, borrado de sección/curso y avisos al profesor en auto-aceptación. Sin WebSockets ni email/push; polling 30s con TanStack Query; limpieza perezosa de leídas antiguas (>30 días) al listar.
- **Notificaciones (implementación):** el payload de cada `Notification` es JSON desnormalizado (ids/títulos/curso/sección) y el frontend lo rinde con `notificationMeta` (label, badge de color y ruta de navegación por `type`). Los triggers viven dentro de los servicios (`grading/services.py` y `course/views.py`), respetando el status final y `is_published`; no hay background jobs (decisión). La API está scoped a `request.user.notifications` (404 en ajenas), con `unread-count` para el badge y `read-all` con conteo actualizado; el cleanup borra leídas con más de 30 días al listar. Tests: 21 (triggers por flujo, alcance por usuario, filtros y sweep).
- **Observabilidad (decisión):** in-house, sin Sentry — `ErrorLog` + custom `EXCEPTION_HANDLER` + `POST /api/errors/` + `ErrorBoundary` frontend.
- **Observabilidad (implementación):** el `EXCEPTION_HANDLER` de DRF envuelve el default y solo persiste excepciones no esperadas (5xx); los errores esperados (400/401/403/404/405/429) conservan la respuesta estándar. Todo 500 responde JSON estable con `error_id` público de soporte y guarda traceback solo en la BD. El reporte del frontend es `POST /api/errors/` con throttle propio, validación (al menos kind/message/stack), truncados y sin aceptar el body del request; `GET /api/errors/` (lista sin traceback) y `GET /api/errors/<id>/` (detalle con traceback/metadata) son superuser-only. En el frontend, `ErrorBoundary` + listeners `error`/`unhandledrejection` (con dedupe por firma, máx. 5 cada 10 s, se ignora AbortError/CanceledError y offline) reportan sin romper la app; la consola admin muestra la lista con filtro por fuente y el detalle con traceback.
- **CI/CD (decisión):** pipeline de verificación en GitHub Actions (lint, tests backend, test/build frontend) que corre en PRs y push; **sin deploy automático** (Render despliega al mergear `main`).
- **CI (fix):** el bloque de seguridad de producción (`if not DEBUG:` con `SECURE_SSL_REDIRECT`/HSTS/cookies Secure) se saltaba en los tests CI porque `DEBUG` no está definido, y el `SecurityMiddleware` redirigía a HTTPS cada request del test client con 301 (210 failures + 19 errors). Se corrigió abriendo el bloque solo con `if not DEBUG and "test" not in sys.argv` (mismo idiom ya usado para el throttle): producción y local quedan inalterados.
- **Refactor backend (fase 6, parte B):** capa de servicios de enrollment en `course/services.py` (`create_enrollment`, `approve_enrollment`, `reject_enrollment`, `delete_enrollment` con notify + audit + limpieza de equipos; los errores de estado lanzan `EnrollmentInvalidStateError` y las views lo mapean al mismo `Response` 400 de antes, la API no cambia), helper `is_teacher(user)` cacheado en `course/permissions.py` (sustituye los `user.groups.filter(name="Teacher")` de course/teams/assignments/grading/authentication; la caché de grupos se invalida con la señal m2m de `authentication/signals.py`), y paginación unificada: `config/pagination.py` define `ListPagination` (15), usada en activity/errors de admin y notificaciones (`ActivityPagination` eliminado). +7 tests del servicio de enrollment en `course/tests.py`.
- **Revisión arquitectura (punto 2):** la estructura de apps (course, authentication, grading, teams, assignments, notifications + config) es sólida y se mantiene; los **estados del curso** ya quedan cubiertos por visibilidad + `is_active` + snapshots por borrado (no hace falta `is_closed`); la **estandarización de respuestas 4xx** (algunos `Response({...}, 4xx)` ad-hoc) se marcó aquí como mejora futura y se implementó después en `ac036e2` (ver sección «Respuestas de error API»). El **design system** señalado ahí como pendiente se implementó después por fases independientes (ver nota `Design system` más abajo).
- **Tests backend:** 305 tests en 7 apps (auth 51, course 85, notifications 21, assignments 29, teams 49, grading 64, config 6), incluidos 14 de observabilidad/throttles, 21 de notificaciones, 7 del servicio de enrollment, +19 de blindaje de concurrencia/IDOR/cierre de cursos, +2 de alcance de profesor (profe no puede unirse/inscribirse en cursos ajenos ni propios, ni siquiera públicos) y 1 de regresión del handler de errores. **Tests frontend:** Vitest + React Testing Library (81 tests: autenticación/interceptor CSRF, AuthProvider, notificationMeta, queryKeys, ProtectedRoute, useGradeMutations, GradesReportPage con validación 0..max + exports, hooks de admisión/join, estados de curso archivado (CourseCard/useToggleCourseActive/EnrollmentSection/CourseDetailHeader), DashboardPage admin con impersonación (clic en "Impersonados recientemente" → modal de confirmación → `startImpersonation`) y primitivas del design system Modal/SelectField/TextareaField/Button). El reporte `frontend/coverage/` está en `.gitignore` (no se sube). Durante los tests de la página de reporte se detectó y corrigió un import faltante (`getSectionGradesReport`) que rompía la página en runtime.
- **TanStack Query / cache / polling:** implementados (polling de cursos a 30s, solo pestaña enfocada).
- **Design system (fases 1–4, commits `a97265d`, `67bcf93`, `854b399`, `d96e7f9`):** primitivas en `src/shared/components/ui/` — `Modal` (overlay, cierre por Escape/backdrop/×, `role="dialog"` + `aria-modal`, tamaños md/lg), `SelectField`/`TextareaField` (misma API que `InputField`: `label/name/register/error/helpText/className` + `...rest`; labels asociados con `useId()`, prop `compact` para filtros de toolbar) y `Button` (variantes primary/secondary/outline/soft/neutral/danger/success/ghost, tamaños sm/md/lg). **Botones:** ~27 primarios `bg-indigo-600` + rojos/verdes/grises inline → `<Button>` en ~26 archivos; se dejaron intencionalmente como `<Link>`-botón las páginas de error (Forbidden/Unauthorized/NotFound), el CTA de Landing y el botón "Instalar" del banner oscuro, además de controles de menú/icono de la Navbar. **Modals:** 7 modals de formulario + confirm de `AdminUsersPage` + historial de notas del panel de profesor + `ConfirmModal` reescritos sobre `Modal` (`ConfirmModal` cambió `confirmClassName` → `confirmVariant`, default `"danger"`). **Selects/textareas:** los 23 selects y 4 textareas restantes → `SelectField`/`TextareaField`; solo el select compacto de `Pager` y `FullScreenLoader` se mantienen especializados. Los exports emerald de `GradesReportPage` mapean a `variant="soft"`/`"success"` (cambio de tono sutil). Accesibilidad: labels vinculados con `htmlFor`/`id` vía `useId`. El refactor además redujo chunks (p. ej. GradesPage 24.21→23.02 kB, CourseDetailPage 26.05→24.03 kB). Suite: 81 tests frontend / 305 backend / lint / build todos verdes.
- **Test de carga (Locust):** `DISABLE_THROTTLE=1` (busca en `config/settings.py`) desactiva los rate limits vía env para medir capacidad real; útil también para cargas del frontend durante el desarrollo. Los throttles por view (`LoginThrottle`, `AdminThrottle`, etc.) ya respetan `settings.DISABLE_THROTTLE`. Escenarios en `backend/loadtest/locustfile.py` (estudiante/profesor/admin con peso, login con doble-envío CSRF vía `GET /auth/csrf/` + `POST /auth/login/`, JWT Bearer). Dev-deps añadidas: `locust` y `waitress` (**gunicorn es Unix-only**, no corre en Windows local; para pruebas locales se usa waitress como WSGI thread-based). Resultados de ejemplo (Walk test local, 15 usuarios, 3 min, sin throttles): **1 instancia/16 threads** → 1.821 req, ~10.1 req/s, p95 ~50 ms, 0% errores; **2 instancias/8 threads** con la misma carga → ~9.8 req/s totales, p95 ~43 ms, 0% errores. Conclusión: a 15 usuarios el API está lejos de saturarse (una sola instancia sobra y el balanceo no aporta); el login cuesta ~300-450 ms (hash Argon/PBKDF2) y es el endpoint más lento; el POST de enrollment mostró una cola larga (hasta ~12 s) en una corrida, señal de la ruta de escritura (lock de fila + auditoría + notificaciones) como siguiente cuello a observar si se sube la concurrencia. **Sonda de saturación (100 usuarios, 1 instancia/16 threads, throttles OFF, 2.5 min):** 8.669 req, **0% errores**, ~58 req/s, p50 ~100 ms, **p95 ~1.1 s** (media 243 ms). La latencia se degrada fuerte desde ~50-60 usuarios (percentiles de lecturas pasan de ~50 ms a >500 ms), y el login sube a ~2 s de media por el hash de contraseña; por eso el **techo operativo útil** está en ~50-80 usuarios simultáneos (o ~30-60 req/s) en esta máquina local, no por errores sino por tiempos de respuesta.

### Cambios e implementaciones — Design system

| Fase | Alcance | Archivos | Tests / verificación | Commit |
|------|---------|----------|----------------------|--------|
| **1 — Primitivas** | `Modal`, `SelectField`, `TextareaField` nuevos; labels accesibles con `useId()` (`htmlFor`/`id`); variantes `soft`/`neutral`/`success` en `Button` | `ui/Modal.jsx`, `ui/SelectField.jsx`, `ui/TextareaField.jsx`, `ui/Button.jsx` + 3 archivos de test | +16 tests primitivas → suite 47→63; lint/build OK | `a97265d` |
| **2 — Botones** | ~27 botones inline (`bg-indigo-600`, `bg-red-600`, `bg-green-600`, `bg-gray-100`, `bg-indigo-50`) → `<Button>` en ~26 archivos; `ConfirmModal` `confirmClassName` → `confirmVariant` (default `danger`) | 27 archivos (`features/*` + `shared/components`) | 63 tests; build OK (chunks más chicos) | `67bcf93` |
| **3 — Selects y textareas** | 23 selects + 4 textareas → `SelectField`/`TextareaField`; prop `compact` para filtros de toolbar; cleanup exports emerald→`soft`/`success` | 18 archivos (admin, assignments, courses, grades, teams) | +2 tests `compact` → 65 tests; lint/build OK | `854b399` |
| **4 — Modals** | 7 modals de formulario (assignments/courses/teams) + confirm inline `AdminUsersPage` + modal historial de notas `TeacherGradingPanel` + `ConfirmModal` → `<Modal>` (beneficio extra: cierre por Escape/backdrop) | 10 archivos | 65 tests; lint/build OK | `d96e7f9` |
| **5 — Verificación y docs** | Suite completa (backend 266, frontend 65, lint, build) + `NEWFEATURES.MD` (Design system ✅, contadores actualizados) | `NEWFEATURES.MD` | ✅ backend 266 OK · frontend 65 OK · lint sin errores · build OK | `8af0d15` |

**Excepciones intencionales** (no se tocaron): `<Link>`-botón de `ForbiddenPage`/`UnauthorizedPage`/`NotFoundPage`, CTA de `LandingPage`, botón "Instalar" de `InstallBanner`, iconos/ítems de `Navbar`, select compacto de `Pager`, `FullScreenLoader`.

### Cambios e implementaciones — Respuestas de error API (4xx)

| Alcance | Archivos | Tests / verificación | Commit |
|---------|----------|----------------------|--------|
| Estandarizar los `Response({...}, status.HTTP_4xx)` ad-hoc de `course` y `notifications` al patrón DRF (`ValidationError`/`PermissionDenied`), de modo que toda respuesta 4xx tenga `detail` estable y el frontend (`getErrorMessage`) las presente uniformemente | `config/errors.py`, `course/views.py`, `course/tests.py`, `notifications/views.py`, `notifications/tests.py` (+1 test regresión handler) | suite backend OK | `ac036e2` |

### Cambios e implementaciones — Blindaje de concurrencia, IDOR de cursos y cierre de cursos

| Alcance | Archivos | Tests / verificación | Commit |
|---------|----------|----------------------|--------|
| Serializar writes concurrentes: `create_enrollment` (course/services.py) y `grade_team`/`grade_student` (grading/services.py) toman `select_for_update()` sobre la fila del curso/assignment; backstop `IntegrityError` → mismo error de negocio (400) en backends sin row locks | `course/services.py`, `course/views.py`, `grading/services.py` | tests `TransactionTestCase` con 2 threads (`Barrier`): join×2 → {201,400} y 1 enrollment; enroll×2 → {201,400} y 1 enrollment (auto-accept off y on → APPROVED); grade×2 → {200} y 1 grade | `67b60d3` + `23ed2f8` |
| **Cierre de curso = bloqueo**: archivar bloquea calificar (student/team, `PermissionDenied`, sin duplicar `Grade`), join (404)/enroll (403) e inscribirse, y toda gestión de equipos (create, add member, edit, delete, change_leader → 403) | `course/services.py`, `course/views.py`, `teams/views.py` (`manager_actions` + `get_object`) | `ArchiveCourseTests` +2, `ArchivedCourseGradingTests` (incl. restaurar vuelve a habilitar), `ArchivedCourseTeamTests` | `67b60d3` |
| IDOR de cursos: editar/borrar curso ajeno → 404 y el curso no se modifica | `course/tests.py` (`CourseForeignOwnershipTests`) | PUT/DELETE sobre `/api/courses/{ajeno}/` → 404 | `67b60d3` |

**Notas:**
- Se corrigió `create_enrollment` para que esté envuelto en `@transaction.atomic` (sin él, `select_for_update` lanzaba `TransactionManagementError` y el endpoint respondía 500; con `ATOMIC_REQUESTS` desactivado el servicio lo necesita explícito).
- Los workers de los tests de concurrencia cierran su conexión DB en `finally` (`connections.close_all()`) para no dejar la BD de test de PostgreSQL bloqueada en el teardown.
- Los tests usan la BD real (PostgreSQL), donde `select_for_update` sí serializa; el manejo de `IntegrityError` es el backstop para backends sin row locks. Los tests con threads (`join`/`enroll`/`grade` concurrentes) se saltan en backends sin row locks (`@skipUnless(vendor=="postgresql")`): SQLite no soporta row locks y dos writers hacen `database is locked` (OperationalError, no IntegrityError) en vez de serializar, así que en CI (SQLite) quedan skipped; el invariant de "1 enrollment/grade por alumno" está cubierto igual por la unique constraint en local/Postgres.
- Backend: **305 tests OK** (más los blindajes previos: 2 join/enroll archivado, 2 IDOR, 3 joins/enrolls concurrentes (join, enroll, enroll auto-accept), 4 grading archivado/concurrente, 6 equipos archivado).
- UX de curso archivado (frontend): `EnrollmentSection` no ofrece "Inscribirme" ni carga secciones cuando `is_active=false` (texto "Este curso está archivado y no acepta nuevas inscripciones") y `CourseDetailHeader` muestra la insignia "Archivado". Tests: `CourseCard` (badge + kebab Archivar/Restaurar), `useToggleCourseActive` (PATCH `{is_active}`, toasts e invalidación), `CourseDetailArchivedStates` (guard de inscripción + badge). Frontend: **77 tests OK** (65 + 12), lint 0 warnings, build OK.

**Esta lista es el backlog técnico.** Cuando termines algo, decime _"ya terminé X"_ y vamos actualizando y tachando.

---

### Fase 1 — Endurecimiento de seguridad y estabilidad (throttles, validación de params, flujos de cuenta)

| Área | Cambio | Archivos | Tests / verificación |
|------|--------|----------|----------------------|
| **Seguridad** | `AuthThrottle` ahora clavea por **IP** (antes `UserRateThrottle` devolvía cache key `None` para usuarios anónimos, dejando **sin límite efectivo** a `/auth/jwt/refresh/` y `/auth/jwt/blacklist/`, que se autentican por cookie y siempre son anónimos para DRF) | `authentication/throttle.py` | Suite completa OK |
| **Estabilidad** | Query params de `AdminActivityView` (`user_id`, `from`, `to`) se validan y devuelven **400** en vez de 500 (antes un valor no numérico/no fecha lanzaba `ValueError` que el `EXCEPTION_HANDLER` registraba como 500 ensuciando `ErrorLog`) | `authentication/views.py` (helpers `_clean_int_param`/`_clean_date_param`) | +4 tests `AdminActivityFilterTests` |
| **Estabilidad** | `GET /api/grades/evolution/?course=` valida que sea entero → **400** (antes 500) | `grading/views.py` | +1 test |
| **Estabilidad** | Creación de equipo con `section_id` no numérico → **400**; `remove_member` con `student_id` no numérico → **404** (antes 500) | `teams/views.py` | +2 tests |
| **Testing** | Cobertura de los **flujos de cuenta** (críticos de seguridad, sin cobertura): registro (usuario creado, rol Student por señal, password con hash, login posterior, duplicado de email, contraseña débil/vacía), edición de perfil (`PATCH /auth/users/me/`, requiere auth) y cambio de contraseña (`set_password` con current_password correcto/incorrecto, requiere auth) | `authentication/tests.py` | +11 tests `AccountFlowTests` |
| **Testing** | Test del nuevo throttle por IP: refresh cortado con 429 sin flag y sin 429 con `DISABLE_THROTTLE` | `authentication/tests.py` | +2 tests `RefreshThrottleTests` |

Backend: **364 tests OK** (los 4 concurrentes de Postgres siguen skipped en SQLite). El endurecimiento de seguridad: (1) refresh/logout ya no quedan sin límite efectivo, (2) los query/body/URL params inválidos devuelven 400/404 estables en vez de 500 que llenaban el `ErrorLog`, y (3) los flujos de cuenta (registro, perfil, contraseña) por fin tienen cobertura. Pendiente del resto de fases para otra iteración: batch-loading de N+1 en dashboard/reportes; migración de hooks manuales a TanStack Query; descomposición de `TeacherGradingPanel`; y accesibilidad de `Modal`/`KebabMenu`. La parte de CI (escaneo de dependencias, `makemigrations --check` y cobertura) se completó en la **Fase 2** (sección siguiente).

### Fase 2 — Calidad de CI: dependencias, migraciones, cobertura y tests de páginas críticas

| Área | Cambio | Archivos | Tests / verificación |
|------|--------|----------|----------------------|
| **Dependencias (backend)** | `pip-audit` sobre `requirements.txt` detectaba **14 vulnerabilidades en 4 paquetes**; se aplicaron los parches del mismo major: `Django 6.0.7→6.0.8`, `djangorestframework 3.17.1→3.17.2`, `sqlparse 0.5.5→0.6.0` y `cryptography 49.0.0→50.0.0` (cadenas verificadas: cryptography←social-auth-core, sqlparse←Django, DRF←simplejwt/djoser). `requirements.txt` (fuente de verdad de CI/build.sh/Render) y `Pipfile`+lock quedan sincronizados | `backend/requirements.txt`, `backend/Pipfile`, `backend/Pipfile.lock` | `pip-audit -r requirements.txt` → **0 hallazgos**; suite backend **364 OK** con las versiones parcheadas |
| **Dependencias (frontend)** | `npm audit` detectaba 3 **high**: `nanoid <3.3.18` y `react-router/-dom 7.12.0–7.18.1`. `npm audit fix` (solo `package-lock.json`): nanoid 3.3.19, react-router 7.18.3 | `frontend/package-lock.json` | `npm audit --audit-level=high` → **0 vulnerabilidades**; 137 tests OK |
| **Migraciones** | `makemigrations --check` (nuevo gate de CI) detectó que el modelo `CourseSettings.ponderacion_enabled` tenía un `help_text` distinto al de la migración `0010` (desfase entre modelo y BD). Se generó la migración `0012_alter_coursesettings_ponderacion_enabled` (solo metadata, no destructiva) y se aplicó a la BD local | `backend/course/migrations/0012_...py` | `makemigrations --check --dry-run` → exit 0 |
| **Cobertura backend** | Patrón de coverage con **workers paralelos**: `.coveragerc` (source apps de negocio, `parallel=True`, omit tests/migrations/apps/admin) + `sitecustomize.py` en `backend/` (activa `coverage.process_startup()` solo si `COVERAGE_PROCESS_START` está en el entorno) → en `manage.py test --parallel` cada worker vuelca su data y `coverage combine` la fusiona (18 archivos combinados) | `backend/.coveragerc`, `backend/sitecustomize.py`, `.gitignore` (+`.coverage*`, `htmlcov/`) | **89%** en la combina paralela; gate CI `--fail-under=85` |
| **Cobertura frontend** | `npm run coverage` (vitest v8) con **thresholds** en `vite.config.js` (statements 20 / branches 15 / functions 15 / lines 20); reporte text+html (binario `frontend/coverage/`, gitignored) | `frontend/vite.config.js` | ~25% statements; pasar el run no rompe (verificación local OK) |
| **CI** | Job backend: `pip install` + `coverage` + `pip-audit`; `manage.py check`; `makemigrations --check --dry-run`; tests con coverage (`coverage run … test --parallel` → `combine` → `report --fail-under=85`) + artefacto `backend/coverage.json`; `pip-audit -r requirements.txt`. Job frontend: `npm ci`; `npm audit --audit-level=high`; lint; `npm run coverage`; artefacto `frontend/coverage`; build | `.github/workflows/ci.yml` | — |
| **Dependabot** | Config creada: npm semanal (grupos react y testing), pip semanal (grupos django y auth), github-actions mensual; límite 5 PRs por ecosistema | `.github/dependabot.yml` | efectivo al pushear a GitHub |
| **Páginas críticas (frontend)** | Tests RTL de las páginas de **Login** (envío y navegación a dashboard, validación de vacíos sin llamar al server, 401 → "Usuario o contraseña incorrectos"), **Register** (payload sin `confirmPassword` + navegación a /login, contraseñas no coinciden, errores de servidor por campo), **CourseDetail** (loading/error/no encontrado, vista profesor propietario con secciones docentes, vista estudiante con inscripción), **Teams** (loading/error/vacío, profesor con filtros y botón nuevo, estudiante sin filtros, flujo de eliminación con confirmación) y **TeamDetail** (loading/error/no encontrado, acciones solo con permisos) | `frontend/src/features/{auth,courses,teams}/pages/__tests__/` (5 archivos) | +24 tests → suíte frontend **137 OK** (27 archivos); lint 0 · build OK |

### PR — Mejoras de UX, notificaciones y acceso administrativo (backlog)

| Prioridad | Área             | Tarea                                                                                  | Estado       |
| --------- | ---------------- | -------------------------------------------------------------------------------------- | ------------ |
| P0        | Acceso admin     | Corregir ruta del Admin Dashboard (404)                                                | ✅ Hecho     |
| P0        | Acceso admin     | Autorización backend del panel (solo rol/permiso admin, no confiar en React)           | ✅ Hecho     |
| P0        | Acceso admin     | Feature flag `ADMIN_PANEL_ENABLED` vía `.env` (no sustituye autorización)              | ✅ Hecho     |
| P0        | Acceso admin     | Pruebas: autorizado / sin permisos / no autenticado / flag deshabilitado               | ✅ Hecho     |
| P1        | Calificaciones   | Toast de éxito/error al guardar nota individual y fuera de Team                        | ✅ Hecho     |
| P1        | Calificaciones   | Estado loading + evitar envíos duplicados (multi-clic)                                 | ✅ Ya cubierto (`beginSave`/`savingKey`) |
| P1        | Notificaciones   | Fetch inicial en el primer render del Dashboard (contador no depende del polling)      | ✅ Ya cubierto (fetch inicial + polling 30s) |
| P1        | Notificaciones   | Inicializar unread count antes del polling; el polling solo actualiza                  | ✅ Ya cubierto |
| P1        | Notificaciones   | Preview limitado + acción "Ver todas" (no renderizar todas en el Dashboard)            | ✅ Hecho     |
| P2        | Inscripciones    | Rediseñar CTA de inscripción en Course Detail (Inscribirme / Inscribiendo… / Inscrito / Solicitud enviada) | ✅ Hecho |
| P2        | Inscripciones    | Feedback visual (éxito/solicitud/ya inscrito/error) + anti doble-clic                  | ✅ Hecho     |
| P2        | UX/UI            | Componente `Button` compartido en Super Admin (variantes y tamaños)                    | ✅ Hecho     |
| P1        | Calificaciones   | Evolución de la nota final (gráfica de la serie en el tiempo)                          | ✅ Hecho     |
| P1        | Calificaciones   | Panel de progreso del curso para el profesor (por tarea y por estudiante)              | ✅ Hecho     |
| P1        | Reportes         | Boletín imprimible / PDF (botón imprimir con `@media print`)                           | ✅ Hecho     |
| 🔮 Futuro  | API             | Documentación Swagger/OpenAPI (`drf-spectacular`)                                      | ⏳ Futura (decisión) |

#### Notas de la PR

- **Acceso admin:** `ADMIN_PANEL_ENABLED=true` en `.env`. Si el panel está deshabilitado, nadie accede aunque tenga permisos; la autorización se valida siempre en backend y los no autorizados reciben la respuesta definida por la política del proyecto.
- **Calificaciones:** feedback visible confirmando guardado/error y estado de carga; comportamiento consistente entre nota individual, nota fuera de Team y notas de Team.
- **Notificaciones:** separar carga inicial, unread count, preview limitado y polling (el polling actualiza el estado existente, no lo inicializa).
- **Inscripción (Course Detail):** el CTA refleja cada estado (puede inscribirse, en proceso, completado, pendiente de aprobación, ya inscrito, error).
- **Super Admin UI:** estandarizar tamaño, padding, tipografía, border-radius, estados hover/active/disabled/loading vía componente compartido (variantes Primary/Secondary/Danger/Ghost, tamaños sm/md/lg).
- **Orden de implementación:** P0 (acceso admin) → P1 (calificaciones + notificaciones) → P2 (Course Detail + Super Admin UI).
- **Verificación extra (junto a la PR):** `TeamIsolationTests` en `teams/tests.py` (12 escenarios: no-miembro 404 en team ajeno, teacher de otro curso 404, miembro común 403 en gestión de equipo, leader 200) y cierre de JWT en `RefreshCsrfSyncTests` (rotación + blacklist del token viejo, blacklist en logout, sincronización CSRF). `ADMIN_PANEL_ENABLED` se expone al frontend como `isAdminPanelEnabled` (flag ausente = habilitado) y oculta los ítems admin de la navbar y la ruta `/admin`.
- **Django admin movido a `/django-admin/`:** para eliminar el choque con la ruta `/admin` de la SPA de React (que se veía como 404), el panel de Django/DRF ahora vive en `/django-admin/` (`config/urls.py`, catch-all de la SPA actualizado). `CspMiddleware` excluye `/django-admin/…` de la política CSP estricta (los scripts inline del admin no calzan con los hashes del index.html); API y SPA mantienen CSP. En dev, Vite proxea `/django-admin` hacia el backend. El panel React agrega un link "Admin de Django (base de datos)" (nueva pestaña). Test de regresión: `config/tests.py` (`CspMiddlewareTests`, 3 tests). Independiente siempre del flag `ADMIN_PANEL_ENABLED` y de `IsSuperuser`.

### Mejoras de calificaciones — evolución, progreso y boletín

| Feature | Backend | Frontend | Tests / verificación |
|---------|---------|----------|----------------------|
| **Evolución de la nota final** | Modelo `FinalScoreSnapshot` (`grading/models.py`, curso+estudiante+score+capturado, check 0..100, índice `(course, student, created_at)`) + migración; captura inline en `grading/services.py` tras `grade_student()` y `grade_team()` vía `record_final_score_snapshots()` (recalcula `final_grade_for_student`, solo guarda si hay nota definida); endpoint `GET /api/grades/evolution/?course=<id>` (acción en `GradeViewSet`, scoping por rol: teacher → propios, estudiante → inscripto en curso activo; últimos 30 puntos ascendentes) | `recharts` (dependencia nueva), `gradeService.getGradeEvolution`, `queryKeys.grades.evolution(id)`, hook `useGradeEvolution`, componente `EvolutionChart` (LineChart 0–100) bajo el toggle "Ver evolución de mi nota final" de `StudentCourseGrades.jsx` (`/grades`, vista estudiante) | 6 tests backend (`FinalScoreSnapshotTests`): snapshot en grade_student/grade_team (uno por miembro aprobado), sin snapshot si no hay tarea publicada, re-calificar apila puntos sin borrar, scoping (estudiante ve propio, teacher solo su curso, otro estudiante ve vacío, 401). **Nota:** el historial arranca al desplegar (sin retroactivo). |
| **Progreso del curso (profesor)** | `course/progress.py` (`course_progress()`): por tarea publicada → graded/pending/avg/max/min; por estudiante aprobado → nota final + tareas evaluadas; `overall_avg_final`; reutiliza el patrón `scores_by_pair` de `grading/exports.py` | `courseService.getCourseProgress`, `queryKeys.courses.progress(id)`, hook `useCourseProgress`, componente `CourseProgress` (tarjetas Est./Promedio/Tareas + tabla por tarea con barras) en la rama profesor de `CourseDetailPage.jsx` | 6 tests backend (`CourseProgressTests`): agregados correctos (un calificado de dos), draft excluido, curso vacío zeroed, estudiante → 403, profesor ajeno → 404, 401. |
| **Boletín imprimible / PDF** | — (sin cambios) | Botón "Imprimir / PDF" (`window.print()`) en `GradesReportPage.jsx` (profesor) y `StudentCourseGrades.jsx` (estudiante); reglas `@media print` en `src/index.css` (fondo blanco, ocultar botones + navbar `print:hidden`) | Build + lint. El navegador genera el PDF desde el diálogo de impresión. |

- **Decisiones:** Swagger/OpenAPI (`drf-spectacular`, UI + schema autenticados, eximir `/api/schema/` de la CSP) queda como **futura**: el desarrollo es 1 persona sin consumidores externos; se retoma cuando haya integraciones/cross-origin. La **ponderación de la nota final** está **implementada** (ver abajo) con la opción A: categorías con % que suman 100, activable por curso.

#### Ponderación de la nota final por curso

| Capa | Alcance | Archivos | Tests / verificación |
|---------|---------|----------|----------------------|
| **Modelo (tareas)** | `Assignment` gana `category` (ACUMULADO/EXAMEN) y `parcial` (PRIMERO/SEGUNDO) con defaults retrocompatibles, índice `(course, category, parcial)` y CheckConstraints | `assignments/models.py`, migración `0004` | `AssignmentPonderacionTests` (defaults, crear examen S2, categoría inválida rechazada, PATCH) |
| **Modelo (curso)** | `CourseSettings` gana `ponderacion_enabled` y 4 DecimalFields (P1/P2 × acumulado/examen, default 25.00); `clean()` valida suma = 100 y límites 0..100, y `Meta.constraints` añade un `CheckConstraint` por campo (0..100) como defensa en BD | `course/models.py`, migraciones `0010` y `0011` | `CourseSettingsTests` extendidos (defaults, activar, suma ≠ 100, negativos y >100 rechazados, constraint) |
| **Cálculo** | `grading/final.py` reescrito con delegación: `final_grade_for_student` usa `_effective_settings` (query fresca para evitar la OneToOne cacheada) y deriva a `ponderated_final_grade_for_student` cuando está activada; `ponderated_breakdown_for_student` expone cada componente (type/parcial/pct/average/assignments). Reglas: sin nota cuenta 0; solo tareas publicadas; busca vacía → `None`; redondeo 0.01; **acumulados y exámenes usan la misma fórmula `Σ(score)/Σ(max)×100`**; **nota por parcial** (`ponderated_parcial_scores_for_student`): cada parcial reescala sus propias categorías a 100 — `Σ(pct_cat×avg_cat)/Σ pct_cat`, categoría sin tareas publicadas excluida de numerador y denominador, publicada-sin-calificar cuenta 0; `None` si el parcial no tiene tareas o la ponderación está desactivada | `grading/final.py` | `PonderatedGradeTests` (13 casos: desactivada = promedio simple; avg ponderado P1 79.00; varios exámenes promediados 84.00; bucket de exámenes con máximos distintos 82.67; examen sin nota cuenta 0 → 76.00; bucket vacío omitido 16.00; sin publicadas → None; ejemplo del usuario 15/35/35/15 → 73.25; breakdown por componente y vacío) + `PonderatedParcialScoresTests` (9 casos: perfecto P1 → 100.00, mezcla acúm/examen 75.00, sin calificar → 45.00, categoría sin publicar excluida, única categoría → su propia nota, redondeo, P2 con buckets propios, sin publicadas → None, ponderación desactivada → None) |
| **API** | Dashboard estudiante expone `final_scores` + `final_breakdowns` + **`parcial_scores`** (por curso y por parcial, solo con ponderación activa; valores string)); `grades_report` y el reporte desde snapshot incluyen `final` por estudiante y el reporte en vivo añade `ponderacion_enabled` + `parcial_scores` por alumno; exports live agregan columnas "Parcial 1"/"Parcial 2" cuando hay ponderación (y "Nota final"); serializer de assignments expone category/parcial (también en payload de snapshots); `course_settings` audita los 6 campos. **Validación backend** del serializer: % ≥ 0, % ≤ 100 y suma exacta = 100 (400 con mensaje claro), más `CheckConstraint` en BD | `course/views.py`, `course/serializers.py`, `course/snapshots.py`, `grading/exports.py`, `assignments/serializers.py` + `views.py` | Full suite backend 339 OK (4 skipped) |
| **Frontend** | Formularios de tarea con selects Categoría/Parcial y badges en la lista; sección "Ponderación de la nota final" en `QuickSettingsBar` (toggle + 4 inputs con suma en vivo, guardado, y **colapso automático**: al perder el foco fuera de la tarjeta solo queda el toggle y se reabre clicando la cabecera; un valor fuera de rango **revierte al último válido** al perder el foco); desglose compacto en `StudentCourseGrades` (p. ej. `15% Acum. P1 (77.5%) + …`) y columna "Nota final" en `GradesReportPage`; **notas por parcial** (`Parcial 1: X% · Parcial 2: —`) bajo la nota final del estudiante y columnas "Parcial 1"/"Parcial 2" en el reporte del profesor (solo con ponderación activa) | 12 archivos en `features/assignments|courses|grades` + 3 tests (`GradesReportPage`, `QuickSettingsBar`, `StudentCourseGrades` nuevo) | 102 tests frontend OK · lint 0 · build OK |
| **Demo** | `seed_demo` marca categorías/parciales, activa la ponderación (15/35/35/15) y se verificó en vivo (dashboard con breakdowns, reporte con `final`) | `course/management/commands/seed_demo.py` | Verificación manual contra Postgres dev |

- **Para el usuario:** por tarea se elige Categoría (Acumulado/Examen) y Parcial (1/2). Con la ponderación **desactivada** (default) la nota final se calcula igual que siempre (promedio por puntos de cada tarea publicada). Activada, el curso define qué % vale cada bloque: **acumulados y exámenes** se promedian igual por `Σ(score)/Σ(max)×100` dentro de cada parcial. El panel se colapsa al salir de la tarjeta y un % inválido (>100, negativo, vacío) revierte a su último valor válido al soltar el foco. Además de la **Nota final combinada**, cada parcial se muestra **sobre 100** (`Parcial 1: 75% · Parcial 2: —`) tanto en el dashboard del estudiante como en el reporte del profesor: el parcial reescala sus propias categorías a 100 (una categoría sin tareas publicadas no pesa; una publicada sin calificar cuenta 0), de modo que un curso solo poblado en el parcial 1 lee ese parcial como una nota de 0 a 100 mientras la final combinada queda topeada por los bloques disponibles.
- Los **snapshots** de sección/curso borrado ya congelan la nota final con el criterio vigente al capturar (payload con `category`/`parcial` por tarea); no se reescriben retroactivamente si luego cambia la ponderación. **Fuera de alcance:** las notas por parcial no se congelan en el payload del snapshot (el reporte/export histórico muestra "Nota final" sin columnas de parcial).
- **Ajustes del reporte (ronda 4):** (1) al editar una nota desde `generar reporte` el reporte se **refetchea** tras el guardado optimista, así `Parcial 1`, `Parcial 2`, `Total` y `Nota final` reflejan el nuevo valor (servidor autoritativo, sin duplicar `grading/final.py` en cliente); (2) se eliminó el botón **Imprimir / PDF** del reporte (quedan CSV y Excel; el estudiante conserva el suyo en `Evaluaciones`); (3) en el detalle de curso, junto a "+ Nueva asignación", aparece **"Ver reporte"** (solo profesor dueño) que navega a `/grades/report?course=…&section=…` con la sección activa del sidebar, igual que "Evaluar"; (4) cada encabezado de asignación en el reporte muestra su **parcial/categoría** (`Acum. P1`, `Exam. P2`, siempre visible), para lo cual el payload `assignments` del reporte (vivo y desde snapshot) incluye ahora `category` y `parcial`. Commits: `7fe5045` (backend payload), `4a0ac10` (reporte frontend), `a410754` (botón Ver reporte + test). Backend 339 OK (4 skip) · frontend 102 OK · lint 0 · build OK · CI verde.
- **Eliminación del campo `weight` (ronda 5):** se quitó el **peso por tarea** para simplificar el modelo (decisión de producto): "vale por dos" ahora se expresa subiendo `puntaje máximo`. La fórmula pasa a `Σ(score)/Σ(max)×100` en todos los modos (default y ponderación por curso); el `weight` desaparece de la BD (migración `RemoveField` 0005), del payload de snapshots futuros, de los formularios/lista de tareas, del panel de calificación, del detalle de snapshot y del seed demo. Los snapshots históricos conservan su JSON congelado (el reporte/export los lee sin depender del peso). Backend 336 OK (4 skip) · frontend 107 OK · lint 0 · build OK.
- **Auditoría de acceso — logins (ronda 6):** cada **login exitoso** queda registrado en `EventLog` (`action=login`, `entity_type=user`, actor/target = el usuario, `metadata.roles` con sus grupos). Sirve para saber quién/cuántas veces entra y, junto a los roles, medir la **aceptación por rol** (p. ej. cuántos estudiantes entran). Solo logins reales: los fallidos no crean evento (el throttle ya cubre fuerza bruta) y el `refresh` de sesión tampoco (no es una entrada nueva). La página `Actividad` del admin ya renderizaba y permitía filtrar por "Inicio de sesión" (etiquetas/badge existían en `activityMeta.js`); sólo faltaba el backend que emite el evento. Tests: +3 en `authentication/tests.py` (`LoginAuditTests`).
- **Métricas de acceso por día (ronda 7):** endpoint `GET /auth/admin/login-stats/` (superuser + `AdminThrottle`) que agrega los logins de los últimos N días (default 7, clamp 1..90): **logins totales**, **usuarios únicos** y serie día a día con ambos conteos. De paso **limpia perezosamente** los eventos de login más viejos que la retención (30 días) para que `EventLog` no crezca sin límite. Frontend: tarjeta "Accesos (últimos N días)" arriba de la página `Actividad` del admin (hook `useLoginStats` + `getLoginStats`, mismo patrón manual con AbortController que los demás hooks admin). Esto responde directo "¿están entrando los estudiantes?" (usuarios únicos que entraron). Tests backend +5 (`LoginStatsTests`) · tests frontend +3 (`AdminActivityPage`).